import Link from "next/link"
import styles from "../components/counters.module.css"
import { FaNodeJs, FaReact } from "react-icons/fa"
import { HiOutlineSparkles } from "react-icons/hi2"
import { FiTool } from "react-icons/fi"
import { projects, skillIconMap } from "../lib/projectsData.js"

export function SkillTag({ icon, label }) {
  const Icon = icon ? skillIconMap[icon] : FiTool
  return (
    <button type="button" className={styles.skill_badge} disabled aria-disabled="true">
      <Icon className={styles.skill_icon} aria-hidden />
      <span>{label}</span>
    </button>
  )
}

export function ProjectCard({ project }) {
  return (
    <Link
      href={`/projects/${project.slug}`}
      className={styles.component}
      style={{ textDecoration: "none", display: "block" }}
    >
      <p className={styles.date}>{project.date}</p>
      <h3 className={styles.job_university} style={{ margin: 0, display: "flex", alignItems: "center", gap: "8px" }}>
        <HiOutlineSparkles className={styles.skill_icon} aria-hidden />
        <span>{project.title}</span>
      </h3>
      <div className={styles.skill_row}>
        {project.skills.map((skill) => (
          <SkillTag key={`${project.slug}-${skill.label}`} icon={skill.icon} label={skill.label} />
        ))}
      </div>
    </Link>
  )
}

# Projects

<section style={{ display: "flex", flexDirection: "column", gap: "16px" }}>
  <h2 className={styles.job_university} style={{ display: "flex", alignItems: "center", gap: "8px", marginTop: "12px" }}>
    <FaNodeJs className={styles.skill_icon} aria-hidden />
    <span>Node.js Projects</span>
  </h2>
  <div style={{ display: "grid", gap: "12px", gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))" }}>
    {projects.slice(0, 2).map((project) => (
      <ProjectCard key={project.slug} project={project} />
    ))}
  </div>
</section>

<section style={{ display: "flex", flexDirection: "column", gap: "16px", marginTop: "20px" }}>
  <h2 className={styles.job_university} style={{ display: "flex", alignItems: "center", gap: "8px" }}>
    <FaReact className={styles.skill_icon} aria-hidden />
    <span>React Projects</span>
  </h2>
  <div style={{ display: "grid", gap: "12px", gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))" }}>
    {projects.slice(2, 4).map((project) => (
      <ProjectCard key={project.slug} project={project} />
    ))}
  </div>
</section>
