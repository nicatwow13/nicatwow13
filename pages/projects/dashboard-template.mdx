import ProjectDetail from "../../components/ProjectDetail"
import { projects } from "../../lib/projectsData.js"

# Dashboard Template

<ProjectDetail project={projects.find((p) => p.slug === "dashboard-template")} />
