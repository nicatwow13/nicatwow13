import ProjectDetail from "../../components/ProjectDetail"
import { projects } from "../../lib/projectsData.js"

# AWS Cognito with Node.js

<ProjectDetail project={projects.find((p) => p.slug === "aws-cognito-with-node-js")} />
