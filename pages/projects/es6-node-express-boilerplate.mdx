import ProjectDetail from "../../components/ProjectDetail"
import { projects } from "../../lib/projectsData.js"

# ES6 Node-Express Boilerplate

<ProjectDetail project={projects.find((p) => p.slug === "es6-node-express-boilerplate")} />
