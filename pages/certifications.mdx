import styles from '../components/counters.module.css'
import { FaCertificate } from 'react-icons/fa'

export const certifications = [
  { title: 'AWS Cloud Developer Nanodegree', org: 'Udacity', year: '2023' },
  { title: 'Microsoft Certified: Azure Developer Associate', org: 'Microsoft', year: '2022' },
  { title: 'AWS Machine Learning Foundations', org: 'Udacity', year: '2021' },
  { title: 'Meta Front-End Developer Specialization', org: 'Coursera', year: '2021' },
  { title: 'Responsive Web Design Certification', org: 'freeCodeCamp', year: '2020' },
  { title: 'Docker for Developers', org: 'Udemy', year: '2020' }
]

export const CertificationsGrid = () => {
  return (
    <div
      style={{
        display: 'grid',
        gap: '14px',
        gridTemplateColumns: 'repeat(1, minmax(0, 1fr))'
      }}
    >
      {certifications.map((item) => (
        <article key={`${item.title}-${item.year}`} className={styles.component} style={{ padding: '16px' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: '10px', marginBottom: '8px' }}>
            <div
              style={{
                width: 40,
                height: 40,
                borderRadius: '12px',
                display: 'grid',
                placeItems: 'center',
                background: 'rgba(59,130,246,0.12)'
              }}
            >
              <FaCertificate className={styles.skill_icon} aria-hidden style={{ width: 18, height: 18 }} />
            </div>
            <div>
              <p className={styles.date} style={{ margin: 0 }}>{item.year}</p>
              <p className={styles.job_university} style={{ margin: 0 }}>{item.title}</p>
            </div>
          </div>
          <p className={styles.university_name} style={{ margin: 0, textAlign: 'left' }}>{item.org}</p>
        </article>
      ))}
    </div>
  )
}

export default function CertificationsPage() {
  return (
    <>
      <h1>Certifications</h1>
      <CertificationsGrid />
    </>
  )
}
